class User {
    constructor(username, password, email, avatar, fullName) {
        this.username = username;
        this.password = password;
        this.email = email;
        this.avatar = avatar;
        this.fullName = fullName
    }
}

export { User }